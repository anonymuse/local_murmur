# Local Murmur
A privacy-preserving local voice-to-text application that records audio, runs offline ASR for transcription, and uses a local LLM for punctuation, rewriting, summarization, and structured output generation.
